pipeline {
    agent any
    
    stages {
        stage('Check Versions') {
            steps {
                script {
                    sh "java --version"
                    
                    echo "java --version"

                }
            }
        }
        
    }
}
